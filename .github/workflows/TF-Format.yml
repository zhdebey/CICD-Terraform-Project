name: Format TF on Push to Main

on:
  push:
    branches:
      - main

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: terraform
      uses: hashicorp/setup-terraform@v3
      run: terraforn fmt -check


